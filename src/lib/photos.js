export async function uploadOrderPhoto({file,orderId,apiBase='http://localhost:8787'}){const fd=new FormData();fd.append('file',file);const url=`${apiBase}/api/upload/order-photo?orderId=${encodeURIComponent(orderId)}`;const res=await fetch(url,{method:'POST',body:fd});if(!res.ok)throw new Error('UPLOAD_FAILED');const data=await res.json();if(!data?.ok||!data?.url)throw new Error('UPLOAD_BAD_RESPONSE');return data.url;}
